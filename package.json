{
  "name": "@adamjosefus/qr-payment",
  "version": "1.0.1",
  "description": "Package for generating QR code content for payment in Czech Republic.",
  "keywords": [
    "qr",
    "payment",
    "spayd",
    "qr platba"
  ],
  "license": "ISC",
  "author": "Adam Josefus",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "format": "biome format ./src",
    "prepublishOnly": "npm run validate && npm run format && npm run build",
    "test": "vitest run ./src",
    "test:watch": "vitest dev ./src",
    "validate": "tsc --noEmit",
    "validate:watch": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "biome": "^0.3.3",
    "typescript": "^5.3.2",
    "vitest": "^0.34.6"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "dependencies": {}
}
